/** Test-owned experimental Client plugin registered through the real bundle protocol. */
window.__ModuleLoader__.load({
  id: '@deepseek-ai/dsh-experimental-isolation-probe',
  factory() {
    return { apply() {} }
  },
})
