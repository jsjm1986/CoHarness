# 安全

[English](SAFETY.md) | 中文

## 发布前状态

CoHarness 处于发布前阶段，尚未接受安全审计。不要把它视为完整的安全边界，也不要默认生产部署就是安全的。

本项目可以执行模型生成的命令与代码、加载第三方插件、访问部署提供的文件和进程，并可选访问外部网络。模型错误、缺陷、配置错误、恶意输入或不可信插件可能修改或删除文件、泄露数据或凭据，或损坏宿主机。

## 沙箱和授权限制

沙箱、审批、Gateway 认证、项目 ACL 和凭据隔离可以降低风险，但不能保证隔离。它们无法保护部署主动开放的资源；被攻陷的插件或宿主机也可能破坏上层假设。macOS 部署不提供 Linux mount namespace 边界。

不要把 CoHarness 作为不可信工作负载唯一的安全控制措施。优先使用一次性虚拟机或专用主机、最小权限账户、受限凭据、网络出站控制和经过测试的备份。

## 负责运行

- 启用前检查插件、Profile、patch、命令和模型路由。
- 除非端点、脱敏、限流、审计和回滚策略获批，否则保持 WebFetch、插件 metadata 和 Session-log upload 关闭。
- 在迁移或激活 release 前备份可访问的文件和数据库，并测试恢复。
- Gateway 必须位于 TLS 之后并使用自身认证流程；不要暴露未认证的 runtime 端口。
- 监控日志、进程树、出站请求、存储增长和失败的授权决策。

## 不提供保证

使用 CoHarness 的风险由使用者自行承担。本软件依照 MIT License 提供，不附带任何保证。在法律允许的最大范围内，作者和贡献者不对使用本项目造成的损坏、数据丢失、凭据泄露或其他损害承担责任。
