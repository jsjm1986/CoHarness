/** Read-only Schedule catalog plugin, node half. The browser entry owns the UI. */

/** Host plugin body — Schedule catalog behavior exists only in the browser entry. */
export function apply(): void {}
