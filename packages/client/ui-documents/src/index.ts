/**
 * `@deepseek-ai/dsh-client-ui-documents`
 *
 * Workspace document manager UI for browsing, previewing, uploading, and
 * deleting documents uploaded through the conversation input box.
 *
 * Host half — exists only for Loader roster discovery.
 * @module plugin
 */

export function apply(): void {}
